'use strict';

/**
 * admin-subquestion service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::admin-subquestion.admin-subquestion');
